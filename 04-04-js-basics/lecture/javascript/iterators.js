// Check ruby/iterators.rb

const fruits = ["Apple", "Pineapple", "Grapes"]

fruits.forEach((fruit) => {
  console.log(fruit);
});

// Note the syntax
  // array.forEach((element) => {
  //   block
  // });
