// Check ruby/classes.rb

// Types
  // string
  // number (instead of Integer AND Float)
  // boolean (instead of TrueClass and FalseClass)
  // null (instead of nil)
  // undefined
  // array
  // object (instead of Hash)


// Number to string
(42).toString();

// String to number
// Note: second argument tells that we are parsing
      // the string to a base 10 number
Number.parseInt('42', 10)
